<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MediMeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MediMeApp" parent="Base.Theme.MediMeApp" />

    <!-- Big pill-shaped bubble button with rounded, bubbly font -->
    <style name="Widget.MediMeApp.BubbleButton" parent="Widget.Material3.Button">
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:letterSpacing">0.02</item>
        <item name="cornerRadius">32dp</item>
        <item name="android:paddingStart">32dp</item>
        <item name="android:paddingEnd">32dp</item>
    </style>
</resources>